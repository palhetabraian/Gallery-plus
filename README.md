# Gallery Plus

## Getting Started

Install dependencies:

```bash
pnpm install
```

Run the backend server:

```bash
pnpm dev-server
```

In another terminal, run the frontend server:

```bash
pnpm dev
```
